import { onFire } from 'stackunderflow/helpers/on-fire';
import { module, test } from 'qunit';

module('Unit | Helper | on fire');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = onFire([42]);
  assert.ok(result);
});
